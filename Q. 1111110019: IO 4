#include <stdio.h>
#include <math.h>
int main() 
{  float a,b,c,d,dist;
  scanf("%f",&a);
 scanf("%f",&b);
 scanf("%f",&c);
 scanf("%f",&d);
 dist=sqrt(pow((a-c),2)+pow((b-d),2));
printf("The distance between two points is=%0.2f units",dist);
	
	return 0;
}
