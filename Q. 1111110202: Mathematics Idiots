#include <stdio.h>
int main() 
{ float a,b,c,d,e,f;
 scanf("%f",&a);
 scanf("%f",&b);
 scanf("%f",&c);
 scanf("%f",&d);
e=(a+c)/2;
 f=(b+d)/2;
 printf("Binoys house is located at (%0.1f , %0.1f)",e,f);
  return 0;
}
